# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Ci-test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
# env:
#   DB_HOST: cyrkl-postgres                      
#   DB_DRIVER: postgres
#   API_SECRET: 98hbun98h                            
#   DB_USER: root
#   DB_PASSWORD: apirestgo
#   DB_NAME: cyrkl
#   DB_PORT: 5432
  
jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.19

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
